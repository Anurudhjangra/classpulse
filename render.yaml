services:
  - type: web
    name: classpulse
    runtime: node
    rootDirectory: classpulse/backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MYSQL_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production